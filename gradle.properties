org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Optional pose JSON upload endpoint.
# If empty, pose JSON is only exported locally to app files/pose_outputs.
POSE_UPLOAD_URL=
POSE_UPLOAD_BEARER=
